# Semgrep Dependencies Export Tool Configuration
# Copy this file to .env and fill in your actual values

# Required: Semgrep API Token
# Get this from your Semgrep account settings
SEMGREP_APP_TOKEN=your_semgrep_api_token_here

# Required: Semgrep Deployment ID
# Find this in your Semgrep dashboard
SEMGREP_DEPLOYMENT_ID=your_deployment_id_here

# Required: Semgrep Deployment Slug
# Used for fetching repository names via Projects API
# Find this in your Semgrep dashboard (different from deployment_id)
SEMGREP_DEPLOYMENT_SLUG=your_deployment_slug_here

# Optional: Output directory for generated files
# If not specified, files will be saved to ./output directory
SEMGREP_OUTPUT_DIR=./reports

# Optional: Specific output file path (overrides OUTPUT_DIR)
# If not specified, a default filename with timestamp will be used
# SEMGREP_OUTPUT_PATH=semgrep_dependencies_report.xlsx

# Optional: Bad license types to highlight in red
# Comma-separated list of license names to flag as problematic
# Case-insensitive matching
SEMGREP_BAD_LICENSES=GPL-3.0,AGPL-3.0,LGPL-2.1,Commercial,Proprietary

# Optional: Review license types to highlight in yellow
# Comma-separated list of license names that require review/attention
# Case-insensitive matching
SEMGREP_REVIEW_LICENSES=MIT,Apache-2.0,BSD-2-Clause

# Optional: Generate report for LICENSE_POLICY_SETTING_BLOCK dependencies
# Set to true to enable generating a separate XLSX file for dependencies
# that have LICENSE_POLICY_SETTING_BLOCK policy setting
# Valid options: true, false, 1, 0, yes, no, on, off
# Default: false
# SEMGREP_POLICY_LICENSES_BLOCK=true

# Optional: Generate report for LICENSE_POLICY_SETTING_COMMENT dependencies
# Set to true to enable generating a separate XLSX file for dependencies
# that have LICENSE_POLICY_SETTING_COMMENT policy setting
# Valid options: true, false, 1, 0, yes, no, on, off
# Default: false  
# SEMGREP_POLICY_LICENSES_COMMENT=true

# Optional: Generate report for PyPI ecosystem dependencies
# Set to true to enable generating a separate XLSX file for dependencies
# that belong to the PyPI ecosystem
# Valid options: true, false, 1, 0, yes, no, on, off
# Default: false
# SEMGREP_ECOSYSTEM_PYPI=true

# Optional: Logging level
# Valid options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# Logs are automatically saved to ./logs/semgrep_export_{deployment_id}_{timestamp}.log
# SEMGREP_LOG_LEVEL=INFO